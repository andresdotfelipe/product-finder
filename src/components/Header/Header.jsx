import SearchBar from '../SearchBar';

const Header = () => {
    return (
        <header>
            <h1>The Product Finder</h1>
            <SearchBar />
        </header>
    );
}

export default Header;